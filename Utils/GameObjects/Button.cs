using UnityEngine;

namespace RunLogger.Utils.GameObjects
{
    internal static class Button
    {
        //internal static UnityEngine.UI.Button Init(bool active)
        //{
        //    if (!((Component)__instance).transform.TryGetChild("Root", out var child)
        //        || !child.TryGetChild("Tabs", out var child2)
        //        || !child2.TryGetChild("Main", out var child3) || !__instance.TryGetOptionTab("Main", out var tab) || !__instance.TryGetOptionTab("Preference", out var tab2))
        //    {
        //        return;
        //    }
        //    Transform val = ((Component)tab).transform.Find("LeftPanel");
        //    Transform val2 = ((Component)tab).transform.Find("RightPanel");
        //    Transform obj = ((Component)tab2).transform.Find("RightPanel");
        //    dropdownTemplate = ((Component)val.Find("Resolution")).gameObject;

        //    GameObject original = j.Find("HintLevel")).gameObject;
        //    GameObject gameObject = Object.Instantiate<GameObject>()
        //}
    }
}